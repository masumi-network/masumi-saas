/**
 * When false, the earnings “over time” UI (dashboard sparkline + /earnings chart) is hidden.
 * Re-enable after the payment service returns reliable `DailyIncome` day keys (see TODO on
 * `GET /api/earnings` implementation).
 */
export const EARNINGS_TIME_SERIES_CHART_ENABLED = false;
